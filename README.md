# BookingManager

A booking management service for shared resources such as meeting rooms and equipment. The backend is built with .NET Web API and PostgreSQL (via EF Core), and the frontend is a Next.js/TypeScript application.
